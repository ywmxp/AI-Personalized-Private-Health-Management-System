# ==========================================
# AI Health 前端环境变量
# ==========================================
# 默认直连真实后端，需要 Mock 时改为 true
# VITE_MOCK_API=true  → 使用模拟数据（后端未就绪时）
# VITE_MOCK_API=false → 调用真实后端 API（默认）
VITE_MOCK_API=false

# VITE_MOCK_AUTH 独立控制登录注册 mock
VITE_MOCK_AUTH=false
